#!/usr/bin/env bash

if [[ $2 ]]; then
    remote_command="tmux a -t $2 || tmux new -s $2"
else
    remote_command="tmux a || tmux new"
fi

if ligo-proxy-test; then
    command=gsissh
else
    command=ssh
fi

$command -Xt $1 "$remote_command"
